{
  "name": "env-strict",
  "version": "0.1.0",
  "description": "Strict validation for your environment variables - fail fast when configuration is wrong",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "env-strict": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "keywords": [
    "environment",
    "env",
    "validation",
    "config",
    "dotenv",
    "typescript",
    "schema"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^25.0.3",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3"
  }
}
